image: stablebaselines/stable-baselines3-cpu:latest

type-check:
  script:
  - make type

pytest:
  script:
    # TODO: remove when atari-py will be in the docker image
  - pip install -e .[extra,docs,tests]
  - make test

doc-build:
  script:
  - make doc
